const {assert} = require('chai');
const app = require('../app.js');

describe('app', async function () {
  it.skip('should get aud btc rate', async function () {
    //const actual = await app.getRate('BTC', 'AUD');
    //assert.isTrue(actual > 0);
  });
});